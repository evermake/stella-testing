An error occurred during typechecking!
Type Error Tag: [ERROR_NOT_A_FUNCTION]
expected a one-argument function type but got
  fn (fn (Bool) -> Nat, Nat) -> fn (Bool) -> Bool
for the expression
  fn (f : fn (Bool) -> Nat, b : Nat)
{
  return fn (a : Bool)
  {
    return true
  }
}

in the expression
  fix (fn (f : fn (Bool) -> Nat, b : Nat)
{
  return fn (a : Bool)
  {
    return true
  }
}
)