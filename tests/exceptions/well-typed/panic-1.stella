language core;

extend with
 #natural-literals,
  #let-bindings,
  #panic;

fn main(n : Nat) -> Nat {
  return
    let n = (succ (2)) in
    let m = (succ (3)) in
    if Nat::iszero(m) then panic! else (succ (0))
}
